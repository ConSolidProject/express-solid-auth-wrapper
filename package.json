{
  "name": "express-solid-auth-wrapper",
  "version": "0.0.1",
  "description": "Middleware functions to authenticate to a remote solid service. Merely a wrapper around '@solid/access-token-verifier' and 'solid-node-client'. ",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeroen Werbrouck",
  "license": "ISC",
  "dependencies": {
    "@solid/access-token-verifier": "^1.0.1",
    "solid-node-client": "^2.1.0"
  }
}
